services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.bot
    env_file:
      - .env
    volumes:
      - ./data:/app/bot/data
    ports:
      - "127.0.0.1:${PORT:-8080}:${PORT:-8080}"


